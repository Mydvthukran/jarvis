# Jarvis Frontend

A frontend-only mock UI for a local AI agent dashboard, inspired by a futuristic HUD style.

## Run (React + Vite)

From the `fronntend` folder:

1. `npm install`
2. `npm run dev`

## Build

`npm run build`

`npm run preview`